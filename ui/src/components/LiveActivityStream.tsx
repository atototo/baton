/**
 * LiveActivityStream — 우측 280px 패널
 *
 * 기존 PropertiesPanel을 새 이름으로 리브랜딩.
 * - 기존 Activity 탭 + Properties 탭 모두 보존
 * - "실시간 에이전트 활동 피드" 강조 (헤더 타이틀 업데이트)
 * - 기존 props/context/API 인터페이스 동일
 */

export { PropertiesPanel as LiveActivityStream } from "./PropertiesPanel";

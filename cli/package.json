{
  "name": "atototo",
  "version": "0.2.7",
  "description": "Baton CLI — orchestrate AI agent teams to run a business",
  "type": "module",
  "bin": {
    "atototo": "./dist/index.js"
  },
  "keywords": [
    "baton",
    "ai",
    "agents",
    "orchestration",
    "cli"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/atototo/baton.git",
    "directory": "cli"
  },
  "homepage": "https://github.com/atototo/baton",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "node --input-type=module -e \"import esbuild from 'esbuild'; import config from './esbuild.config.mjs'; await esbuild.build(config);\" && chmod +x dist/index.js",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "@atototo/adapter-claude-local": "workspace:*",
    "@atototo/adapter-codex-local": "workspace:*",
    "@atototo/adapter-cursor-local": "workspace:*",
    "@atototo/adapter-opencode-local": "workspace:*",
    "@atototo/adapter-openclaw": "workspace:*",
    "@atototo/adapter-utils": "workspace:*",
    "@atototo/db": "workspace:*",
    "@atototo/server": "workspace:*",
    "@atototo/shared": "workspace:*",
    "drizzle-orm": "0.38.4",
    "dotenv": "^17.0.1",
    "commander": "^13.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
